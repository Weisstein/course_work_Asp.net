@model Build
@{
    ViewData["Title"] = "AddBuild";
}
<div class="d-grid gap-0">
    <div class="mb-3">
        <label class="form-label">Наименование сборки</label>
        <input class="form-control border-secondary bg-dark text-secondary" />
    </div>
    <div>
        <label class="form-label">Описание сборки</label>
        <textarea class="form-control border-secondary bg-dark text-secondary" rows="3"></textarea>
    </div>
</div>
<div>
    <div class="d-grid gap-5">

    </div>
</div>

